@* 可以在视图中使用 @using 表达式来引用命名空间，类似于与标准的 C# 代码中一样，而且可以使用多个该表达式引用多个命名空间*@
@using MyRazorEG.Models
@model Product[]
@*@model MyRazorEG.Models.Product[]*@

@{
    ViewBag.Title = "DemoArray";
    Layout = "~/Views/_BasicLayout.cshtml";
}

@if (Model.Length > 0)
{
    <table>
        <thead><tr><th>Product</th><th>Price</th></tr></thead>
        <tbody>
            @*@foreach(MyRazorEG.Models.Product p in Model)*@
            @foreach (Product p in Model)
            {
                <tr>
                    <td>@p.Name</td>
                    <td>$@p.Price</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h2>No product data</h2>
}